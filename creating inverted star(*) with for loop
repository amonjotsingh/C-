#include <stdio.h>
#include <stdlib.h>

int main()
{
int i,k,j,r;
printf("enter no of rows:");
scanf("%d",&r);
for(i=r;i>=0;i--)
{
	for(j=(r-1);j>=i;j--)
			{
				printf(" ");
			}
	for(k=0;k<(2*i-1);k++)
	{
		printf("*");

	}

	printf("\n");
}
return 0;
}
